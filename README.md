
  # Minimal Landing Page Design

  This is a code bundle for Minimal Landing Page Design. The original project is available at https://www.figma.com/design/Kkas5B3eYri5Fr9P033oie/Minimal-Landing-Page-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  